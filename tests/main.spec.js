// import { expect } from 'chai';
let { expect } = require('chai');

describe('Main', () => {
  describe('Method A', () => {
    it('it should do something', () => {
      expect(true).to.be.true;
      console.log('teste');
    });
  });
});
